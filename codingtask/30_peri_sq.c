#include<stdio.h>
#include "main.h"
void periofsq()
{
	int perimeter, length;
	printf("Enter the length of the square");
	scanf("%d", &length);
	perimeter= 4 * length;
	printf("Perimeter of the Square is : %d\n",perimeter);
}
