#include<stdio.h>
#include "main.h"
void areaofsq()
{
	int area, length;
	printf("Enter the length of square");
	scanf("%d",&length);
	area = length * length;
	printf("Area of the Square : %d", area);
}
